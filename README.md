> **Archived 2026-08-16.** This unfinished static panel prototype is preserved for historical reference only. Development continued separately in [`pc-style/paneldev`](https://github.com/pc-style/paneldev), which is also archived; no code transfer was needed because `panel`'s `dev` branch is identical to `main` and `paneldev` already contains the later prototype work. This repository has no declared license, so no permission to reuse is granted. The complete Git history and all branches are retained.
>
> The prototype contains historical example infrastructure text. Treat it as non-operational sample content, not a current endpoint or configuration.

# panel2
 aa
