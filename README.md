> [!IMPORTANT]
> **Archival prepared 2026-08-16; repository-settings authorization is still required to make this repository read-only.** This unfinished static prototype is retained for history only. Development continued separately in [`pc-style/paneldev`](https://github.com/pc-style/paneldev), whose archival is also prepared; no transfer was needed because `panel`'s `dev` branch is identical to `main` and `paneldev` already contains the later work. No license is declared, so no permission to reuse is granted. Full Git history and both branches are preserved.
>
> The prototype contains historical example infrastructure text. Treat it as non-operational sample content, not a current endpoint or configuration.

# panel2
 aa
